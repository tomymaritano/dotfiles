# nvim — runtime state, not config
# (lazy-lock.json / lazyvim.json ARE tracked, on purpose, for reproducible
#  plugin versions + extras — like a package-lock. theme.txt is per-machine.)
nvim/theme.txt

# macOS
.DS_Store

# never commit secrets / auth
**/auth.json
**/.env
fish_variables
op/secrets.env
